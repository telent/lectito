<%= render partial: "friend_class_leftbar", locals: {section: 'followers'} %>
<div class=friends_list>
<h1> Followers of <%= @user.fullname %></h1>
<%= render :partial=>"row", :collection=> @followers or
  "This user has no followers (yet?)"
%>
</div>
